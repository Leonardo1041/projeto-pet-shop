<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gerenciamento de Produtos - Loja Pet Shop</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      background: white;
      border-radius: 10px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
      overflow: hidden;
    }

    header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 30px;
      text-align: center;
    }

    header h1 {
      font-size: 2.5em;
      margin-bottom: 10px;
    }

    .header-controls {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 20px;
      flex-wrap: wrap;
      gap: 20px;
    }

    .btn-novo {
      background: #4CAF50;
      color: white;
      padding: 12px 25px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 1em;
      font-weight: bold;
      transition: background 0.3s;
      text-decoration: none;
      display: inline-block;
    }

    .btn-novo:hover {
      background: #45a049;
    }

    .alert {
      padding: 15px 20px;
      margin: 20px;
      border-radius: 5px;
      display: none;
    }

    .alert.success {
      background: #d4edda;
      color: #155724;
      border: 1px solid #c3e6cb;
      display: block;
    }

    .alert.error {
      background: #f8d7da;
      color: #721c24;
      border: 1px solid #f5c6cb;
      display: block;
    }

    .content {
      padding: 20px;
    }

    .table-responsive {
      overflow-x: auto;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }

    table thead {
      background: #f5f5f5;
      border-bottom: 2px solid #667eea;
    }

    table th {
      padding: 15px;
      text-align: left;
      font-weight: bold;
      color: #333;
    }

    table td {
      padding: 12px 15px;
      border-bottom: 1px solid #ddd;
    }

    table tbody tr:hover {
      background: #f9f9f9;
    }

    .acoes {
      display: flex;
      gap: 10px;
      align-items: center;
    }

    .btn-editar, .btn-deletar {
      padding: 8px 15px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 0.9em;
      font-weight: bold;
      transition: all 0.3s;
      text-decoration: none;
      display: inline-block;
    }

    .btn-editar {
      background: #2196F3;
      color: white;
    }

    .btn-editar:hover {
      background: #0b7dda;
    }

    .btn-deletar {
      background: #f44336;
      color: white;
    }

    .btn-deletar:hover {
      background: #da190b;
    }

    .mensagem-vazia {
      text-align: center;
      padding: 50px 20px;
      color: #999;
      font-size: 1.1em;
    }

    .stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }

    .stat-card {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 20px;
      border-radius: 8px;
      text-align: center;
    }

    .stat-card h3 {
      font-size: 2em;
      margin: 10px 0;
    }

    .stat-card p {
      opacity: 0.9;
    }

    @media (max-width: 768px) {
      header h1 {
        font-size: 1.8em;
      }

      .header-controls {
        flex-direction: column;
      }

      table {
        font-size: 0.9em;
      }

      table th, table td {
        padding: 8px 10px;
      }

      .acoes {
        flex-direction: column;
        gap: 5px;
      }

      .btn-editar, .btn-deletar {
        width: 100%;
        text-align: center;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1>🐾 Gerenciamento de Produtos</h1>
      <p>Controle completo da loja Pet Shop</p>
      <div class="header-controls">
        <a href="/loja/novo" class="btn-novo">+ Novo Produto</a>
      </div>
    </header>

    <div class="content">
      <% if (typeof query !== 'undefined' && query.success) { %>
        <div class="alert success">
          ✓ <%= query.success %>
        </div>
      <% } %>
      <% if (typeof query !== 'undefined' && query.error) { %>
        <div class="alert error">
          ✗ <%= query.error %>
        </div>
      <% } %>

      <% if (produtos.length > 0) { %>
        <div class="stats">
          <div class="stat-card">
            <p>Total de Produtos</p>
            <h3><%= produtos.length %></h3>
          </div>
          <div class="stat-card">
            <p>Preço Médio</p>
            <h3>R$ <%= (produtos.reduce((sum, p) => sum + p.preco, 0) / produtos.length).toFixed(2).replace('.', ',') %></h3>
          </div>
          <div class="stat-card">
            <p>Produto Mais Caro</p>
            <h3>R$ <%= Math.max(...produtos.map(p => p.preco)).toFixed(2).replace('.', ',') %></h3>
          </div>
        </div>

        <div class="table-responsive">
          <table>
            <thead>
              <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>Tipo</th>
                <th>Preço</th>
                <th>Descrição</th>
                <th>Ações</th>
              </tr>
            </thead>
            <tbody>
              <% produtos.forEach(produto => { %>
                <tr>
                  <td>#<%= produto.id %></td>
                  <td><strong><%= produto.nome %></strong></td>
                  <td><span style="background: #e3f2fd; padding: 4px 8px; border-radius: 4px;"><%= produto.tipo %></span></td>
                  <td><strong>R$ <%= parseFloat(produto.preco).toFixed(2).replace('.', ',') %></strong></td>
                  <td><%= produto.descricao || '-' %></td>
                  <td>
                    <div class="acoes">
                      <a href="/loja/editar/<%= produto.id %>" class="btn-editar">✏️ Editar</a>
                      <form method="POST" action="/loja/deletar/<%= produto.id %>" style="display: inline;" onsubmit="return confirm('Tem certeza que deseja deletar este produto?');">
                        <button type="submit" class="btn-deletar">🗑️ Deletar</button>
                      </form>
                    </div>
                  </td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      <% } else { %>
        <div class="mensagem-vazia">
          <h2>📦 Nenhum produto cadastrado</h2>
          <p>Clique em "+ Novo Produto" para adicionar um produto à loja.</p>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>