<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Galeria de Produtos - Loja Pet Shop</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 1400px;
      margin: 0 auto;
    }

    header {
      background: white;
      color: #333;
      padding: 30px;
      border-radius: 10px;
      text-align: center;
      margin-bottom: 30px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    }

    header h1 {
      font-size: 2.5em;
      margin-bottom: 10px;
      color: #667eea;
    }

    .header-controls {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 20px;
      flex-wrap: wrap;
    }

    .btn-novo {
      background: linear-gradient(135deg, #4CAF50 0%, #45a049 100%);
      color: white;
      padding: 12px 25px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 1em;
      font-weight: bold;
      text-decoration: none;
      display: inline-block;
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .btn-novo:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(76, 175, 80, 0.4);
    }

    .stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }

    .stat-card {
      background: white;
      padding: 20px;
      border-radius: 10px;
      text-align: center;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      border-left: 5px solid #667eea;
    }

    .stat-card h3 {
      font-size: 2em;
      color: #667eea;
      margin: 10px 0;
    }

    .stat-card p {
      color: #666;
    }

    .gallery {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }

    .product-card {
      background: white;
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s, box-shadow 0.3s;
      display: flex;
      flex-direction: column;
    }

    .product-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
    }

    .product-image {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      height: 200px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 4em;
      color: #ddd;
    }

    .product-info {
      padding: 20px;
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    .product-id {
      color: #999;
      font-size: 0.85em;
      margin-bottom: 5px;
    }

    .product-name {
      font-size: 1.3em;
      font-weight: bold;
      color: #333;
      margin-bottom: 8px;
    }

    .product-tipo {
      display: inline-block;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 0.85em;
      margin-bottom: 12px;
      width: fit-content;
    }

    .product-description {
      color: #666;
      font-size: 0.95em;
      margin-bottom: 12px;
      flex: 1;
    }

    .product-price {
      font-size: 1.8em;
      font-weight: bold;
      color: #4CAF50;
      margin-bottom: 15px;
    }

    .product-actions {
      display: flex;
      gap: 10px;
    }

    .btn-editar, .btn-deletar {
      flex: 1;
      padding: 10px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-weight: bold;
      text-decoration: none;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 5px;
      transition: all 0.3s;
      text-align: center;
    }

    .btn-editar {
      background: #2196F3;
      color: white;
    }

    .btn-editar:hover {
      background: #0b7dda;
    }

    .btn-deletar {
      background: #f44336;
      color: white;
    }

    .btn-deletar:hover {
      background: #da190b;
    }

    .alert {
      padding: 15px 20px;
      margin-bottom: 20px;
      border-radius: 5px;
      display: none;
      animation: slideDown 0.3s ease-in;
    }

    .alert.success {
      background: #d4edda;
      color: #155724;
      border: 1px solid #c3e6cb;
      display: block;
    }

    .alert.error {
      background: #f8d7da;
      color: #721c24;
      border: 1px solid #f5c6cb;
      display: block;
    }

    @keyframes slideDown {
      from {
        transform: translateY(-20px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    .empty-state {
      text-align: center;
      padding: 60px 20px;
      background: white;
      border-radius: 10px;
      margin-top: 30px;
    }

    .empty-state h2 {
      font-size: 2em;
      color: #667eea;
      margin-bottom: 10px;
    }

    .empty-state p {
      color: #999;
      margin-bottom: 20px;
    }

    .icon-dog {
      font-size: 4em;
      margin: 20px 0;
    }

    @media (max-width: 768px) {
      header h1 {
        font-size: 1.8em;
      }

      .gallery {
        grid-template-columns: 1fr;
      }

      .stats {
        grid-template-columns: 1fr;
      }

      .header-controls {
        flex-direction: column;
      }

      .btn-novo {
        width: 100%;
      }

      .product-actions {
        flex-direction: column;
      }

      .btn-editar, .btn-deletar {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1>🐾 Galeria de Produtos Pet Shop</h1>
      <p>Visualize e gerencie todos os produtos</p>
      <div class="header-controls">
        <a href="/loja/novo" class="btn-novo">+ Adicionar Novo Produto</a>
      </div>
    </header>

    <% if (typeof query !== 'undefined' && query.success) { %>
      <div class="alert success">
        ✓ <%= query.success %>
      </div>
    <% } %>
    <% if (typeof query !== 'undefined' && query.error) { %>
      <div class="alert error">
        ✗ <%= query.error %>
      </div>
    <% } %>

    <% if (produtos && produtos.length > 0) { %>
      <div class="stats">
        <div class="stat-card">
          <p>📦 Total de Produtos</p>
          <h3><%= produtos.length %></h3>
        </div>
        <div class="stat-card">
          <p>💰 Preço Médio</p>
          <h3>R$ <%= (produtos.reduce((sum, p) => sum + p.preco, 0) / produtos.length).toFixed(2).replace('.', ',') %></h3>
        </div>
        <div class="stat-card">
          <p>💎 Produto Mais Caro</p>
          <h3>R$ <%= Math.max(...produtos.map(p => p.preco)).toFixed(2).replace('.', ',') %></h3>
        </div>
        <div class="stat-card">
          <p>🏷️ Produto Mais Barato</p>
          <h3>R$ <%= Math.min(...produtos.map(p => p.preco)).toFixed(2).replace('.', ',') %></h3>
        </div>
      </div>

      <div class="gallery">
        <% produtos.forEach(produto => { %>
          <div class="product-card">
            <div class="product-image">
              <% 
                const tipos = {
                  'Alimento': '🍖',
                  'Acessório': '👑',
                  'Brinquedo': '🎾',
                  'Higiene': '🧼',
                  'Saúde': '💊',
                  'Roupa': '👕',
                  'Outro': '📦'
                };
                const icon = tipos[produto.tipo] || '📦';
              %>
              <%= icon %>
            </div>
            <div class="product-info">
              <div class="product-id">#<%= produto.id %></div>
              <div class="product-name"><%= produto.nome %></div>
              <span class="product-tipo"><%= produto.tipo %></span>
              <div class="product-description">
                <%= produto.descricao || 'Sem descrição' %>
              </div>
              <div class="product-price">
                R$ <%= parseFloat(produto.preco).toFixed(2).replace('.', ',') %>
              </div>
              <div class="product-actions">
                <a href="/loja/editar/<%= produto.id %>" class="btn-editar">✏️ Editar</a>
                <form method="POST" action="/loja/deletar/<%= produto.id %>" style="flex: 1;" onsubmit="return confirm('Tem certeza que deseja deletar este produto?');">
                  <button type="submit" class="btn-deletar" style="width: 100%;">🗑️ Deletar</button>
                </form>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="empty-state">
        <div class="icon-dog">🐕</div>
        <h2>Nenhum Produto Cadastrado</h2>
        <p>Comece adicionando seus primeiros produtos à loja.</p>
        <a href="/loja/novo" class="btn-novo">+ Adicionar Primeiro Produto</a>
      </div>
    <% } %>
  </div>
</body>
</html>
